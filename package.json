{
  "name": "retail-pos",
  "version": "1.0.0",
  "private": true,
  "description": "RetailPOS — Point of Sale System",
  "main": "apps/pos/electron/main.js",
  "scripts": {
    "install:all": "npm install --prefix apps/pos/backend && npm install --prefix apps/pos/frontend && npm install --prefix apps/license-server && npm install --prefix apps/website && npm install --prefix apps/admin-dashboard/frontend && npm install --prefix apps/admin-dashboard/backend",
    "dev:backend": "npm run dev --prefix apps/pos/backend",
    "dev:frontend": "npm run dev --prefix apps/pos/frontend",
    "dev:website": "npm run dev --prefix apps/website",
    "dev:admin-dashboard": "npm run dev --prefix apps/admin-dashboard/frontend",
    "dev:admin-dashboard-backend": "npm run dev --prefix apps/admin-dashboard/backend",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "build:backend": "npm run build --prefix apps/pos/backend",
    "build:frontend": "npm run build --prefix apps/pos/frontend",
    "build:website": "npm run build --prefix apps/website",
    "build:admin-dashboard": "npm run build --prefix apps/admin-dashboard/frontend",
    "build:admin-dashboard-backend": "npm run build --prefix apps/admin-dashboard/backend",
    "build:all": "npm-run-all build:backend build:frontend",
    "electron:dev": "npm run build:all && electron .",
    "electron:build": "npm run build:all && electron-builder --win",
    "electron:build:all": "npm run build:all && electron-builder --win --mac --linux",
    "license-server:setup": "npm run setup --prefix apps/license-server",
    "license-server:dev": "npm run dev --prefix apps/license-server",
    "license-server:build": "npm run build --prefix apps/license-server",
    "license-server:start": "npm run start --prefix apps/license-server"
  },
  "dependencies": {
    "embedded-postgres": "18.3.0-beta.17"
  },
  "devDependencies": {
    "electron": "^31.0.2",
    "electron-builder": "^24.13.3",
    "javascript-obfuscator": "^5.4.2",
    "jimp": "^1.6.1",
    "npm-run-all": "^4.1.5",
    "png2icons": "^2.0.1",
    "sharp": "^0.34.5"
  }
}

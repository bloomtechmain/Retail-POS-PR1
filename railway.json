{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --prefix apps/pos/backend && npm install --prefix apps/pos/frontend && npm run build --prefix apps/pos/backend && npm run build --prefix apps/pos/frontend"
  },
  "deploy": {
    "startCommand": "node apps/pos/backend/dist/app.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
